# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# 🟢 [W-RT-DETR-L]
# 基于频域分析的微小目标检测架构
# 修改点：Backbone 下采样层替换为 FrequencyAwareFusion

# Parameters
nc: 10 # 🔴 注意：VisDrone 只有 10 类，这里我已经帮你改好了 (原版是80)
scales:
  l: [1.00, 1.00, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, HGStem, [32, 48]] # 0-P2/4
  - [-1, 6, HGBlock, [48, 128, 3]] # stage 1

  # === 🟢 修改 1: 第一次下采样 (P3) ===
  # 原版: [-1, 1, DWConv, [128, 3, 2, 1, False]]
  # 说明: [out_channels=128]
  - [-1, 1, FrequencyAwareFusion, [128]] # 2-P3/8

  - [-1, 6, HGBlock, [96, 512, 3]] # stage 2

  # === 🟢 修改 2: 第二次下采样 (P4) ===
  # 原版: [-1, 1, DWConv, [512, 3, 2, 1, False]]
  # 说明: [out_channels=512]
  - [-1, 1, FrequencyAwareFusion, [512]] # 4-P3/16

  - [-1, 6, HGBlock, [192, 1024, 5, True, False]] # cm, c2, k, light, shortcut
  - [-1, 6, HGBlock, [192, 1024, 5, True, True]]
  - [-1, 6, HGBlock, [192, 1024, 5, True, True]] # stage 3

  # === 🟢 修改 3: 第三次下采样 (P5) ===
  # 原版: [-1, 1, DWConv, [1024, 3, 2, 1, False]]
  # 说明: [out_channels=1024]
  - [-1, 1, FrequencyAwareFusion, [1024]] # 8-P4/32

  - [-1, 6, HGBlock, [384, 2048, 5, True, False]] # stage 4

head:
  - [-1, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 10 input_proj.2
  - [-1, 1, AIFI, [1024, 8]]
  - [-1, 1, Conv, [256, 1, 1]] # 12, Y5, lateral_convs.0

  # 🔥🔥🔥 修改点 1: P5 -> P4 上采样 🔥🔥🔥
  # 原版: - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [-1, 1, WaveletUpsample, [256]] # 13 (替换为可学习的上采样)

  - [7, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 14 input_proj.1
  - [[-2, -1], 1, Concat, [1]]
  - [-1, 3, RepC3, [256]] # 16, fpn_blocks.0
  - [-1, 1, Conv, [256, 1, 1]] # 17, Y4, lateral_convs.1

  # 🔥🔥🔥 修改点 2: P4 -> P3 上采样 🔥🔥🔥
  # 原版: - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [-1, 1, WaveletUpsample, [256]] # 19 (替换为可学习的上采样)

  - [3, 1, Conv, [256, 1, 1, None, 1, 1, False]] # 20 input_proj.0 (注意原来的索引可能会变，但 -1 引用是相对的，所以没事)
  - [[-2, -1], 1, Concat, [1]] # cat backbone P4
  - [-1, 3, RepC3, [256]] # X3 (22), fpn_blocks.1

  - [-1, 1, Conv, [256, 3, 2]] # 23, downsample_convs.0
  - [[-1, 17], 1, Concat, [1]] # cat Y4
  - [-1, 3, RepC3, [256]] # F4 (25), pan_blocks.0

  - [-1, 1, Conv, [256, 3, 2]] # 26, downsample_convs.1
  - [[-1, 12], 1, Concat, [1]] # cat Y5
  - [-1, 3, RepC3, [256]] # F5 (28), pan_blocks.1

  - [[22, 25, 28], 1, RTDETRDecoder, [nc]] # Detect(P3, P4, P5)